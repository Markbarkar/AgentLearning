## 优化知识库获取
目前知识库获取是存在两条路径：
1. 在agent.run()的阶段就将知识库的相关信息加入到上下文
2. 提供search_knowledge_base工具主动查询知识库

但是目前的需求在于用户能够控制是否让agent利用知识库，防止信息干扰，采用直接过滤工具好像比较绝对，要么就直接移除rag工具要么就不要过滤
因此大致想的方案是把search_knowledge_base工具作为agent的一种回想方式，通过训练让agent减少主动调用该工具

最终该考虑到严谨性，还是采用了过滤方案

## prompt优化
当前是使用的prompts是两层的，main_prompt负责循环，final_prompt负责最终输出
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Agent 完整执行流程                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  用户输入: "用图列举你知道的所有知识库信息"                                   │
│                           ↓                                                 │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    阶段1: ReAct 循环 (多轮)                            │  │
│  │                    使用: main_prompt                                   │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │  │  Round 0:                                                       │  │  │
│  │  │  - Think: 需要检索知识库 → 选择 search_knowledge_base 工具       │  │  │
│  │  │  - Act: 执行工具                                                 │  │  │
│  │  │  - Observe: 获取结果                                             │  │  │
│  │  ├─────────────────────────────────────────────────────────────────┤  │  │
│  │  │  Round 1:                                                       │  │  │
│  │  │  - Think: 信息已足够 → 选择 FINISH                               │  │  │
│  │  └─────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                       │  │
│  │  输出: 结构化的 Action JSON (给程序用，用户看不到)                     │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                           ↓                                                 │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    阶段2: 最终总结 (单次)                              │  │
│  │                    使用: final_prompt                                  │  │
│  │                                                                       │  │
│  │  输入: 任务描述 + 所有思考/行动记录 (memory)                           │  │
│  │  输出: 用户最终看到的答案 ← 这里决定是 markdown 还是 mermaid！         │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                           ↓                                                 │
│  用户看到的回答: mermaid 图 / markdown 表格                                 │
│                                                                             │
└───────────────────────────────────────────────────────────────────────────

## 意图识别
我注意到现在的意图识别没还有做，只是简单的把用户输入的内容原封不动放到main_propmpt、final_prompt中进行执行


## 关于mermaid的输出不可靠的问题
agent输出的mermaid在大多数情况下输出都是错误的导致没办法渲染