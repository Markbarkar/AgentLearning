{
  "mcpServers": {
    "360-netdisk": {
      "command": "npx",
      "args": ["-y", "@aicloud360/mcp-server-disk"],
      "env": {
        "API_KEY": "${360_NETDISK_API_KEY}"
      },
      "enabled": true,
      "description": "360 网盘工具，api_key获取地址：https://www.yunpan.com/micro/mcp#/"
    }
  }
}

